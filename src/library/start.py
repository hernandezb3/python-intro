import os

# users
USER_DIR = os.getcwd().split("code")[0]

# directory to data folder
DATA_DIR = USER_DIR + "/data/"

# directory to code folder
PYTHON_DIR = USER_DIR + "code/python/"

# directory to library folder
LIBRARY_DIR = PYTHON_DIR + "library/"

RESULTS_DIR = USER_DIR + "results/"